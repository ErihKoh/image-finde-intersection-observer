const searchForm = document.querySelector('#search-form');
const gallery = document.querySelector('.gallery');
const sentinel = document.querySelector('.conteiner');
// const images = document.querySelectorAll('[data-lazy]');

export default {
    searchForm,
    gallery,
    sentinel,
    // images,
};